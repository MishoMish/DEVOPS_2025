apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: devops-demo
  labels:
    app: api-service
    component: backend
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  sessionAffinity: None
