apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: devops-demo
  labels:
    app: web-service
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: web-service
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  sessionAffinity: None
